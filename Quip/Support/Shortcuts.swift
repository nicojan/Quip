import KeyboardShortcuts

extension KeyboardShortcuts.Name {
    /// Global shortcut that opens (toggles) the Quip popover. User-configurable
    /// in Settings; clearing it disables the global shortcut.
    static let summonQuip = Self("summonQuip")
}
